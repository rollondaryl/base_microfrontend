{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:host": "yarn workspace host start",
    "dev:dashboard": "yarn workspace dashboard start",
    "start": "lerna run start",
    "build": "lerna run build",
    "lint": "lerna run lint"
  },
  "dependencies": {
    "@babel/core": "^7.28.0",
    "@babel/preset-env": "^7.28.0",
    "@babel/preset-react": "^7.27.1",
    "babel-loader": "^10.0.0"
  },
  "devDependencies": {
    "lerna": "^7.0.0"
  }
}
